DONE: add zoom and panning

DONE: add more color palettes
	(default) black/white gradient
	(done)    hue gradient (full)
	(done)    hue gradient (red)
	(done)    hue gradient (green)
	(done)    hue gradient (blue)

DONE: vectorize the computation

DONE: don't use DrawPixel,
	(done) because of memory caching (steps and view stuff)
	(done) because you can use a texture instead with a static buffer for the pixels

TODO: maybe implement multithreading
